# Use an official Nginx image to serve the frontend
FROM nginx:alpine

# Copy the current frontend files to the Nginx container
COPY . /usr/share/nginx/html

# Expose the port the frontend will run on
EXPOSE 80
